[build]
  command   = "echo ok"
  publish   = "."
  functions = "netlify/functions"
  # Skip deploy if only index.html changed — saves build credits
  ignore    = "git diff --quiet HEAD~1 HEAD -- netlify/"

[build.environment]
  NODE_VERSION = "20"

[functions."sync-acessorias"]
  schedule = "0 9 * * *"

# Netlify Functions ficam em /.netlify/functions/* — não redirecionar
[[redirects]]
  from   = "/.netlify/functions/*"
  to     = "/.netlify/functions/:splat"
  status = 200

# SPA fallback — tudo mais vai para index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
